import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.chanot',
  appName: 'mon-projet-ionic-chanot',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
